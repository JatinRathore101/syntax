server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: secret
  application:
    name: my-spring-app

app:
  max-retries: 3
  timeout: 5000
  features:
    - authentication
    - logging
    - caching
